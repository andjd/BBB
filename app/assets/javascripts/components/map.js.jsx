(function(root) {
  'use strict';

    var BBB = root.BBB = (root.BBB || {});

    BBB.Map = React.createClass({
      render: function () {
        return(
          <div className="map" />
        );
      }
    });

}(this));
