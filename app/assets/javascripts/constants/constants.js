(function(root) {
  'use strict';
    var BBB = root.BBB = (root.BBB || {});

    BBB.Constants = {
      ALL_BENCHES_RECD: "ALL_BENCHES_RECD",
      ONE_BENCH_RECD: "ONE_BENCH_RECD"
    };
}(this));
